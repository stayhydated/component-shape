interface:
  display_name: "Use Component Shape GPUI"
  short_description: "GPUI shape macros and builders"
  default_prompt: "Use $use-component-shape-gpui to declare or review GPUI component shapes, configured builders, render metadata, and value binding."
